version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.11"

python:
  version: "3.11"
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs

mkdocs:
  configuration: mkdocs.yml
  fail_on_warning: true
